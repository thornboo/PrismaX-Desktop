{
  "name": "@prismax/ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "dependencies": {
    "@prismax/shared": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "type-check": "tsc -p tsconfig.json --noEmit"
  }
}

